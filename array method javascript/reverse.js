// reverse for The Array.reverse() method reverses the order of the elements in an array.

const fruits = ["Banana", "Orange", "Apple", "Mango"];

console.log('sebelum data di reverse', fruits)
let text = fruits.reverse();
console.log("sesudah data di reverse", fruits);

let arr = [
    { 'actionPlan': 'edwin', 'reason': 'Auliano', 'targetDate': '23/06/2022' },
    { 'actionPlan': 'team', 'reason': 'tur', 'targetDate': '23/06/2021' },
];

console.log('sebelum data di reverse', arr)
let text2 = arr.reverse();
console.log("sesudah data di reverse", arr);