const users = {
    name: "Edwin",
    ocupation: "model",
    hobbies: "Music"
}

const userArrayKey = Object.keys(users);
const userArrayValues = Object.values(users);

console.log("obj", users);
console.log("obj to array key", userArrayKey);
console.log("obj to array Values", userArrayValues);